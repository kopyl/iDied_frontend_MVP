import { FixFormSizeOnDocumentResizeDirective } from './fix-form-size-on-document-resize.directive';

describe('FixFormSizeOnDocumentResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FixFormSizeOnDocumentResizeDirective();
    expect(directive).toBeTruthy();
  });
});
